📁 Folder Structure (Home Page Scope)
src/
 ├─ components/
 │   ├─ Navbar.jsx
 │   ├─ Hero.jsx
 │
 ├─ pages/
 │   └─ Home.jsx
 │
 ├─ App.jsx
 ├─ main.jsx
 └─ index.css

1️⃣ Navbar.jsx

Sticky, minimal, smooth scroll, dark-mode friendly.

import { useState } from "react";

export default function Navbar() {
  const [open, setOpen] = useState(false);

  return (
    <header className="fixed top-0 left-0 w-full z-50 bg-black/70 backdrop-blur border-b border-white/10">
      <nav className="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <h1 className="text-xl font-semibold text-white">
          nagur<span className="text-indigo-400">.dev</span>
        </h1>

        <ul className="hidden md:flex gap-8 text-sm text-gray-300">
          {["About", "Skills", "Projects", "Contact"].map((item) => (
            <li key={item}>
              <a href={`#${item.toLowerCase()}`} className="hover:text-white">
                {item}
              </a>
            </li>
          ))}
        </ul>

        <button
          className="md:hidden text-white"
          onClick={() => setOpen(!open)}
        >
          ☰
        </button>
      </nav>

      {open && (
        <div className="md:hidden bg-black border-t border-white/10 px-6 py-4">
          {["About", "Skills", "Projects", "Contact"].map((item) => (
            <a
              key={item}
              href={`#${item.toLowerCase()}`}
              className="block py-2 text-gray-300 hover:text-white"
              onClick={() => setOpen(false)}
            >
              {item}
            </a>
          ))}
        </div>
      )}
    </header>
  );
}

2️⃣ Hero.jsx

High-conversion hero. Recruiter impact in 5 seconds.

export default function Hero() {
  return (
    <section className="min-h-screen flex items-center justify-center bg-gradient-to-br from-black via-gray-900 to-black text-white">
      <div className="max-w-4xl px-6 text-center">
        <h1 className="text-4xl md:text-6xl font-bold leading-tight">
          Full Stack <span className="text-indigo-400">Web Developer</span>
        </h1>

        <p className="mt-6 text-gray-400 text-lg md:text-xl">
          I build fast, scalable, and modern web applications using the MERN
          stack.
        </p>

        <div className="mt-10 flex justify-center gap-4 flex-wrap">
          <a
            href="#projects"
            className="px-6 py-3 rounded-xl bg-indigo-500 hover:bg-indigo-600 transition font-medium"
          >
            View Projects
          </a>

          <a
            href="/resume.pdf"
            className="px-6 py-3 rounded-xl border border-white/20 hover:border-white transition font-medium"
            download
          >
            Download Resume
          </a>
        </div>
      </div>
    </section>
  );
}

3️⃣ Home.jsx

Single-page layout (clean, scalable).

import Navbar from "../components/Navbar";
import Hero from "../components/Hero";

export default function Home() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        {/* Next sections: About, Skills, Projects, Contact */}
      </main>
    </>
  );
}

4️⃣ App.jsx
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}